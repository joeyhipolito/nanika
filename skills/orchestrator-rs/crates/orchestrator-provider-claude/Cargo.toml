[package]
name = "orchestrator-provider-claude"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
orchestrator-core = { path = "../orchestrator-core" }
orchestrator-exec = { path = "../orchestrator-exec" }
serde.workspace = true
serde_json = { workspace = true, features = ["raw_value"] }
thiserror.workspace = true

[lints]
workspace = true

[features]
# Compiles ClaudeProvider::new_first_use_pilot for the explicit, non-enrolled
# orchestrator-first-use-pilot binary. Production parsing stays pinned.
experimental-first-use-pilot = []
